[package]
name = "fork-join"
version = "0.1.0"
edition = "2021"

[dependencies]
rayon = "1.5.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
assert-json-diff = "1.0"